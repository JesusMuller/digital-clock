import React from 'react';
import './Footer.less';

const Footer = () => (
    <footer>
        <small>This Clock was created with React, javascript, less, StyledComponents, CssGrid</small>
        <small>CssGrid is <strong>not supported</strong> on IE yet</small>
    </footer>
);

export default Footer;